package com.ifels.controlflex.api;

import net.minecraftforge.fml.common.Mod;

/**
 * Forge / NeoForge 1.20.1 mod entry point.
 * No-op: this mod provides only API interfaces and the static hub class.
 *
 * <p>Uses the old {@code net.minecraftforge.fml.common.Mod} annotation,
 * recognized by both Forge 1.20.1 and NeoForge 1.20.1 (soft fork).</p>
 */
@Mod("controlflex_api")
public final class ControlFlexApiForgeMod {
    // No-op: API mod provides interfaces only.
}
